import Typed from "typed.js";

var options = {
  strings: ["digital experiences", "creative solutions"],
  loop: true,
  fadeOut: true,
  smartBackspace: true,
  typeSpeed: 50,
  showCursor: false,
  fadeOutDelay: 1000,
};

// var typed = new Typed(".hero__title span", options);
